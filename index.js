"use strict";

/**
 *
 * @type {number} - a, b
 */


let a = Number(prompt('введите число a'));

let b = Number(prompt('введите число b'));
if ((a > 2 && a <11) || (b >= 6 && b < 11) ){

    alert ('Верно');
} else {
    alert ( 'Неверно');
}
