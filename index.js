"use strict";

/**
 *
 * @type {number} - a
 */


let a = Number(prompt('введите число a'));

let b = Number(prompt('введите число b'));
if (a <= 1 && b >= 3 ){

    alert ( a + b );
} else {
    alert ( a - b);
}
